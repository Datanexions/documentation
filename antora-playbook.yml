site:
  title: "Datanexions Docs"
  start_page: streams::index.adoc

content:
  sources:
    - url: ./docs/streams
      branches: main

ui:
  bundle:
    url: ./antora-ui-default/build/ui-bundle.zip

output:
  dir: ./build/site